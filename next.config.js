/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

(module.exports = {
  images: {
    domains: ["bit.ly", "lh3.googleusercontent.com"],
  },
}),
  nextConfig;
